<!DOCTYPE html>
<html>
  <head>
    <title>All Users</title>
  </head>
  <body>
    <h1>All Users</h1>
    <% users.forEach((user) => { %>
      <p>Name: <%= user.name %></p>
      <p>Email: <%= user.email %></p>
      <hr>
    <% }) %>
  </body>
</html>